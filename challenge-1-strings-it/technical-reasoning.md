**Note**: command used **strings strings.1 | grep picoCTF**
I used the **strings** command to identify the pattern, plus the tip said **strings**, so I used **strings strings.1**
I used **grep** to find the specific phrase **picoCTF**.
